# Core transcription. faster-whisper alone gives sentence and word timings;
# whisperx adds true CTC forced alignment, which is what the word-level
# teleprompter needs to feel right.
faster-whisper>=1.0.0
yt-dlp>=2024.1.0

# Optional, install if you want word-level forced alignment:
#   pip install whisperx
# Optional, install if you want native pitch contours:
#   pip install librosa soundfile
